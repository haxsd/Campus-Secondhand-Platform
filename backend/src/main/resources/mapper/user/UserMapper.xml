<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.trade.user.mapper.UserMapper">
    <sql id="userColumns">id, student_no, phone, password, nickname, avatar, campus, role, status, created_at, updated_at</sql>
    <select id="selectByAccount" resultType="com.campus.trade.user.entity.User">SELECT <include refid="userColumns"/> FROM `user` WHERE student_no=#{account} OR phone=#{account} LIMIT 1</select>
    <select id="selectById" resultType="com.campus.trade.user.entity.User">SELECT <include refid="userColumns"/> FROM `user` WHERE id=#{id} LIMIT 1</select>
    <select id="existsByStudentNoOrPhone" resultType="boolean">SELECT EXISTS(SELECT 1 FROM `user` WHERE student_no=#{studentNo} OR phone=#{phone})</select>
    <insert id="insert" parameterType="com.campus.trade.user.entity.User" useGeneratedKeys="true" keyProperty="id">INSERT INTO `user` (student_no,phone,password,nickname,avatar,campus,role,status) VALUES (#{studentNo},#{phone},#{password},#{nickname},#{avatar},#{campus},#{role},#{status})</insert>
    <insert id="insertCreditSummary">INSERT INTO user_credit_summary (user_id) VALUES (#{userId})</insert>
    <update id="incrementDealCount">UPDATE user_credit_summary SET deal_count=deal_count+1 WHERE user_id=#{userId}</update>
    <update id="updateProfile" parameterType="com.campus.trade.user.entity.User">UPDATE `user` SET nickname=#{nickname}, avatar=#{avatar}, campus=#{campus} WHERE id=#{id}</update>
    <update id="updatePassword">UPDATE `user` SET password=#{password} WHERE id=#{userId}</update>
    <select id="selectCreditSummary" resultType="com.campus.trade.user.entity.CreditSummary">SELECT user_id, credit_score, deal_count, review_count, avg_rating, good_review_rate, bad_review_count FROM user_credit_summary WHERE user_id=#{userId} LIMIT 1</select>
</mapper>
